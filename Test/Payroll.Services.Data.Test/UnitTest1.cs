using System.Reflection;

namespace Payroll.Services.Data.Test
{
	public class Tests
	{
		[SetUp]
		public void Setup()
		{
		}

		//[Test]
		public void TestConnectionString()
		{

			//var dirPath = Path.GetFullPath( Assembly.GetExecutingAssembly().Location + @"\..\..\..\..\..\Payroll.Data\Services\" );

			//var dirPath = Path.GetFullPath( Assembly.GetExecutingAssembly().Location);
			//Console.WriteLine(dirPath);
			Assert.Pass();
		}
	}
}