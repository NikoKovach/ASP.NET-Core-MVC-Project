@model List<string>?

<datalist id="idDocument-name-list">
       @{
              for ( int i = 0; i < Model.Count; i++ )
              {
                     <option value="@Model[ i ]"></option>
              }
       }
</datalist>
