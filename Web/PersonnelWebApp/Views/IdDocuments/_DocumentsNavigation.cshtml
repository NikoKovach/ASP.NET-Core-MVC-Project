@using Payroll.Services.Services
@using Payroll.ViewModels.PagingViewModels
@using Payroll.ViewModels.PersonViewModels

@model PagingListSorted<IdDocumentVM>

@{
       IdDocumentVM? firstDocument = Model.ItemsCollection[ 0 ];
       int? personId = ( Model.ItemsCollection.Count > 0 ) ? firstDocument.PersonId : null;
}

<div>
       <div class="d-flex form-control justify-content-start p-1 m-0 h-50">
              <div class="d-inline mx-1">
                     <form asp-action="AllDocuments" method="post">
                            <input type="submit" class="btn btn-success border-0 px-1 py-0 col-sm-12" value="..." />
                            <input type="hidden" value=@personId name="personId" />
                     </form>
               </div>
              <div class="d-inline mx-1">
                     <form asp-action="AllDocuments" method="post">
                            <input asp-for="@Model.PageSize" type="text" name="pageSize"
                                   class="border-1 col-md-2 px-1 py-0"/>
                            <label>Table rows</label>
                            <input type="hidden" value=@personId name="personId" />
                            <input type="hidden" value="@Model.PageIndex" name="pageIndex" />
                            <input type="hidden" value=@Model.SortValue name="sortParam" />
                     </form>
               </div>
       </div>
</div>

@* <datalist id="rows-count">
       <option value="2"></option>
       <option value="4"></option>
</datalist> *@