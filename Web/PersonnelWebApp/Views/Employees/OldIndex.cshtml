@* @using Payroll.Services.Services.EmployeeServices
@using Payroll.ViewModels.EmployeeViewModels
@model PaginatedList<GetEmployeeVM> 

@{
	ViewData["Title"] = "Index";
	Layout = "~/Views/Shared/_LayoutLight.cshtml";
}

<div class="col">
	<div class="row navbar-custom">
		<div class="col-md-3">
			<h3 class="text-justify text-dark">Employees</h3>
		</div>
		<div class="col-md-9">
			<nav class="navbar navbar-custom navbar-expand-sm
			navbar-light justify-content-center">
				<ul class="navbar-nav">
					<li class="nav-item">
						<a asp-controller="Employees" asp-action="Create" class="nav-link btn">Add Employee</a>
					</li>
					<li class="nav-item">
						<a href="" class="nav-link btn">Add Person</a>
					</li>
					<li class="nav-item">
						<a href="#" class="nav-link btn">Vacations</a>
					</li>
					<li class="nav-item">
						<a href="#" class="nav-link btn">Disablement</a>
					</li>
				</ul>
			</nav>
		</div>
	</div>
	<hr />
</div>

<div id="employees" class="main-view">
	<div>
		<form asp-action="Index" method="get" class="form-inline">
			<div class="form-control mb-2">
				<div class="row">
					<div class="col-md-5">
						<label>Select Company</label>
						<vc:select-company></vc:select-company>
					</div>
					<div class="col-md-1">
						<button type="submit" class="btn btn-success">Search</button>
					</div>			
				</div>	
			</div>
		</form>
	</div>
	<div>
		<partial name="_PartialEmployee" model="@Model[0]"></partial>
		<br />
		@{
			var prevDisabled = !Model.HasPreviousPage ? "disabled" : "";
			var nextDisabled = !Model.HasNextPage ? "disabled" : "";
		}

		<div class="carousel col-mb-6">
			<a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex - 1)"
			   class="previous-page-button btn btn-outline-primary @prevDisabled">
				Previous</a>

			<input class="emp-paging text-center" name="pageNumber"
			value="@Model.PageIndex" />
			<label>/ @Model.TotalPages</label>

			<a asp-action="Index" asp-route-pageNumber="@(Model.PageIndex + 1)"
			   class="next-page-button btn btn-outline-primary @nextDisabled">
				Next</a>
		</div>
	</div>
	
</div> *@